export enum TodoStatus {
   PENDING = 'pending',
   IN_PROGRESS = 'in_progress',
   DONE = 'done',
   CANCELED = 'canceled',
}

export enum TodoPriority {
   LOW = 'low',
   MEDIUM = 'medium',
   HIGH = 'high',
}
