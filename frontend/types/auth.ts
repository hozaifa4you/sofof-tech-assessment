export enum Role {
   CUSTOMER = "Customer",
   ADMIN = "Admin",
   SUPER_ADMIN = "SuperAdmin",
}

export enum Status {
   ACTIVE = "Active",
   DEACTIVATED = "Deactivated",
   SUSPENDED = "Suspended",
   DELETED = "Deleted",
}